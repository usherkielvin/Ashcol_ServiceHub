<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="app.hub.user.DashboardActivity">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Floating Indicator View -->

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_chatbot"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="26dp"
            android:background="#FD0000"
            android:contentDescription="@string/chatbot_button_description"
            android:forceHasOverlappingRendering="false"
            android:src="@drawable/ic_aichatbot"
            android:translationZ="99999999dp"
            app:backgroundTint="#00FF4141"
            app:elevation="0dp"
            app:layout_constraintBottom_toBottomOf="@+id/fragmentContainerView"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_dodgeInsetEdges="bottom"
            app:layout_gravity="bottom|end"
            app:maxImageSize="56dp"
            app:pressedTranslationZ="0dp"
            app:tint="@null" />

        <!-- New ticket banner - shows in main activity container when returning from ticket creation -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/newTicketBanner"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:layout_margin="12dp"
            app:cardCornerRadius="8dp"
            app:cardElevation="4dp"
            app:cardBackgroundColor="#E8F5E9"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:padding="16dp">

                <ImageView
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/ic_ticket"
                    app:tint="#2E7D32" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical"
                    android:layout_marginStart="12dp">

                    <TextView
                        android:id="@+id/newTicketBannerTitle"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Ticket created"
                        android:textSize="14sp"
                        android:textStyle="bold"
                        android:textColor="#1B5E20" />

                    <TextView
                        android:id="@+id/newTicketBannerId"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text=""
                        android:textSize="12sp"
                        android:textColor="#388E3C" />
                </LinearLayout>

                <TextView
                    android:id="@+id/newTicketBannerAction"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="View"
                    android:textColor="#2E7D32"
                    android:textStyle="bold"
                    android:padding="8dp"
                    android:clickable="true"
                    android:focusable="true" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/fragmentContainerView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toTopOf="@+id/bottomNavigationView"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/newTicketBanner"
            app:layout_constraintVertical_bias="0.0" />

        <View
            android:id="@+id/navIndicator"
            android:layout_width="40dp"
            android:layout_height="4dp"
            android:background="@drawable/nav_indicator_shape"

            android:elevation="18745dp"
            android:translationY="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/bottomNavigationView" />

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigationView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#FFFFFF"
            android:minHeight="80dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            app:elevation="0dp"
            app:itemActiveIndicatorStyle="@null"
            app:itemIconSize="24dp"
            app:itemIconTint="@color/nav_item_color_state"
            app:itemRippleColor="#00FFFFFF"
            app:itemTextColor="@color/nav_item_color_state"
            app:labelVisibilityMode="labeled"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:menu="@menu/usermenu" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/servicebtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="#215531"
        android:contentDescription="Add Service"

        android:src="@drawable/ic_add"
        app:borderWidth="0dp"
        app:elevation="0dp"

        app:fabCustomSize="80dp"
        app:layout_anchor="@id/bottomNavigationView"

        app:layout_anchorGravity="top|center_horizontal"
        app:maxImageSize="40dp"

        app:shapeAppearanceOverlay="@style/ShapeAppearance.ServiceHub.Circle"
        app:tint="@color/white" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
